# Frontend App

React single-page application that renders the chat interface and data artifacts.

## Roadmap

- Initialize Vite + React + TypeScript scaffold.
- Implement chat transcript, composer, and artifact panels.
- Integrate with backend `/chat` endpoint and handle loading/error states.

Work starts after the backend API contract is defined.
