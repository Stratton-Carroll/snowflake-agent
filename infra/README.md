# Infra & Tooling

- `Makefile` (to be added) will provide shortcuts for linting, testing, and running services.
- Containerization (Docker Compose) can be introduced once backend/frontend scaffolds stabilize.
- Keep local secrets in `.env`; avoid committing personal Snowflake credentials.

As the project grows, this directory will collect templates for Docker, CI workflows, and deployment scripts.
